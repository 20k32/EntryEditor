<Page
    x:Class="EntryEditor.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EntryEditor.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="1076" Height="752">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center">
                <TextBlock Text="Enter first name:"
                       VerticalAlignment="Center"/>
                <TextBox Text="tempFirstName"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0, 5">
                <TextBlock Text="Enter last name:"
                       VerticalAlignment="Center"/>
                <TextBox Text="tempLastName"/>
            </StackPanel>
            <Button Content="Add"/>
            <Button Content="Edit"/>
            <Button Content="Save"/>
        </StackPanel>

        <ListBox Grid.Row="1">
            <ListBoxItem HorizontalContentAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" 
                            Background="AntiqueWhite"
                            Grid.Column="0">
                        <TextBlock Text="Bobby bob"
                               HorizontalAlignment="Left"/>
                    </StackPanel>
                    <Button Content="Delete"
                            Grid.Column="1"
                            HorizontalAlignment="Right"/>
                </Grid>
            </ListBoxItem>
        </ListBox>
    </Grid>
</Page>
